namespace mluvii.ApiModels.Users
{
    public enum GlobalRole
    {
        UNKNOWN = 0,
        Admin,
        CompanyAdmin,
        Wallboard,
        CompanyOperatorMetricsView,
        CompanyStatistics
    }
}
